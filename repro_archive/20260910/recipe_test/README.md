# R2 与 C4：三个配对种子的固定协议 Test

## 最终状态（2026-09-10）

六模型Test全部完成并归档，R2三个配对种子的IoU均正向，平均提升 **0.5762个百分点**，Dice平均提升 **0.3784个百分点**。R2 Test IoU/Dice均值为75.9833%/84.5031%，固定阈值0.5。完整逐图结果、每个种子配对和限制见 [结果](results/README.md)。Test结束后约16分钟完成唯一一次最终检查，满足30分钟约定；其产物均已同步HF，heartbeat已删除。以下提交和预估条目保留为历史过程记录。

## 提交时执行状态

2026-09-10 04:29:26.558（悉尼）提交后台，PID182805，评估源码 `285785429e9584797a4130611c4e206244652533`，tag `evaluation-r2-recipe-test-3seeds-20260910`。当前只有启动回执，尚未确认运行健康或取得Test结果。预计04:47:37.777完成，已将同一heartbeat改约05:00进行短连接最终检查；Test预算0/2，不增加训练检查。系统盘可用4,024,750,080字节，共享fs实际18,559,782,256字节，提交前GPU空闲。启动与部署哈希见 [launch.json](launch.json)，实际动态状态以outputs目录为准。

前置三种子 Val 复验已通过：IoU 平均差值 +0.0054396003（+0.5440 个百分点），三个种子均为正，平均 Dice 和最小病灶组 IoU 均不下降。原计划已经授权通过后的 Test 阶段。

本目录将六个 Val IoU 选出的 Best 模型统一评估。每个模型 2113 张 Test 图像、batch16、224 输入、严格概率 >0.5；不训练、不改模型、不挑种子、不按 Test 调阈值或重选 epoch。历史 C4-1219 也重新用同一导出器评估，避免混用不同版本结果。该 Test 集在项目历史开发中已被访问，不能描述为新的独立留出集。

| 种子 | C4 Best | R2 Best |
|---|---:|---:|
| 1219 | 80 | 67 |
| 2027 | 70 | 68 |
| 3407 | 79 | 75 |

完整模型来源 SHA、实验标签、检查点路径和 Val 文件哈希固定在 `test_plan.json`。`three_seed_summary.json` 保留当时 Val 阶段未访问 Test 的历史标记，不能把历史文件改写为当前状态。

`evaluate_test.py` 复用此前已执行的 Test 导出器；模型加载、数据预处理、推理循环和科学指标的 AST 与历史实现一致。新增来源、种子、Best、配方与阶段门检查。`check_test_protocol.py` 只做离线完整性检查，不访问 Test 数据。

执行顺序为 C4-1219 → R2-1219 → C4-2027 → R2-2027 → C4-3407 → R2-3407。`run_test_chain.py` 在服务器后台依次执行，每组有独立日志和结果，全部成功后自动生成三种子 Test 汇总。结果报告所有种子的 macro IoU/Dice、配对差值、均值和样本标准差；逐图 bootstrap 区间不能代替跨种子区间。

四次已完成的 Val 导出实测耗时按 2113/1429 比例换算，再为每个模型增加20秒启动与来源校验余量，预计整个 Test 链约18.2分钟。提交后一次短连接检查约在预测结束后12分钟；不保持 SSH，不设置循环轮询，不新增训练检查。若首次最终检查仍在执行，最多再预约一次有依据的 Test 检查，并明确报告原预测偏差。

操作入口（Windows 本地 Python 加 `-X utf8`）：

1. `prepare_test.py`：仅从已完成 Val 文件生成冻结计划，禁止覆盖。
2. `check_test_protocol.py`：验证指标实现复用及错误来源/阈值/样本等拒绝路径。
3. 提交全部源码和协议，建立独立评估标签并推送 GitHub。
4. `test_control.py launch`：从已提交 Git 字节部署，服务器后台启动；只返回回执。实际来源提交、预测与预约写入 `D:/BetterLViT/outputs/recipe_test_20260910`。
5. 到预约时间运行 `test_control.py collect` 一次；预算先扣，完整结果按远端 SHA256 校验后下载。失败保留日志，不盲目重跑。
6. `finalize_test_archive.py`：仅基于已下载完成结果复算、归档并更新台账，然后提交推送，删除完成的 heartbeat。

模型保持原位置，不复制权重；HF 认证待恢复，当前不重试认证或上传，也不删除模型。训练配方优化不作为 Capstone 的第二项结构创新。
