# Hugging Face 上传恢复（2026-09-10）

## 当前核验结果

九组训练归档已全部完成，共47文件、15,415,489,746逻辑字节；源文件、服务器与独立本地云端列表的大小/Xet哈希全部一致。服务器原模型保留。本轮六模型Test也已完成，另15个JSON/日志/汇总与协议产物已追加云端并核验。总计62个文件、15,421,689,106逻辑字节（约15.42GB）。完整逐文件核验见 [results](results/README.md)。

用户提供的新凭据已通过 `razaxq` 账户验证，已更新标准 Hugging Face 凭据缓存。凭据通过隐藏输入读取，服务器上传只通过标准输入传入；源码、日志和本归档不含 token。

首批六个已完成的80轮训练已全部上传并通过双端云端核验，共30文件、10,156,614,282源文件逻辑字节。每个提交前缀保留会话日志、原始Val评估、Best、Last和TensorBoard事件。Val阶段的未访问Test标记保留其历史含义，本次备份不会将验证结果改称测试结果。

| 实验 | Git前缀 |
|---|---|
| R1 seed1219 | f71e7829 |
| R2 seed1219 | 9eca26de |
| C4 seed2027 | 965382b2 |
| R2 seed2027 | c972a152 |
| C4 seed3407 | 02b28b2d |
| R2 seed3407 | 7b498ba9 |

补充检查发现P11的80轮、150轮续训以及P12尚未云端归档，故也纳入本次补传，合计17文件、5,258,875,464逻辑字节。P11续训的最佳模型仍来自原80轮提交，其Best在云端从已核验的父提交对象复制；续训Last来自新提交。续训前缀额外保留 `runtime.json` 与 `selection.json`，明确父子来源，不能将其误记为从头训练150轮。

| 实验 | Git前缀 | 归档性质 |
|---|---|---|
| P11 80轮 | 2fc6ab5c | 已完成探索实验，Test评估 |
| P11 150轮续训 | c724a620 | 续训完成，Best保留原80轮模型 |
| P12 80轮 | 5d09913d | 已完成Val负结果，无Test主张 |

历史P11文档由快照重新格式化，因此本地JSON与服务器JSON的原始字节可不同。本次先确认内容完全相同，再比较排序后的规范JSON哈希；同时记录两份原始文件哈希。模型来源、实际epochs、Best、seed、选模规则和模型可加载性另行核对，未放宽这些检查。

上传目的地为 Bucket `razaxq/BetterLViT`，顶层仅使用Git短哈希。操作仅添加缺失文件；遇到同名文件必须大小和Xet哈希完全一致，否则停止。原模型不删除。服务器上传沿用AutoDL学术加速、单并发Xet设置并降低CPU/I/O优先级。

## 可复现入口

- `authenticate.py`：隐藏读取新凭据，验证账户后写入标准HF缓存；不要把token放在命令行参数中。
- `upload_pending.py`：校验六组来源文件的新鲜Xet哈希，补传30文件，并用独立本地客户端再次核验。
- `additional_archives.py prepare`：核验P11/P12静态完成证据、模型和来源，生成17文件清单，不轮询训练或Test。
- `additional_archives.py upload`：首批六组完成后补传三个历史完成实验。
- `upload_test_exports.py`：仅在预约Test检查确认六模型全部完成且首批训练归档完成后，添加本轮Test JSON和日志；保留历史评估文件。三个种子的汇总、固定协议与Val阶段门放在评估源码短哈希目录。

本地入口使用 `python -X utf8`。动态记录位于 `D:/BetterLViT/outputs/hf_recovery_20260910`。只有 `*_upload_verified.json` 中的远端路径、字节数及Xet哈希匹配，且独立本地客户端复核通过，才计为完成；上传开始、认证成功或源清单本身都不代表上传完成。
