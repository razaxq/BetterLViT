# D：仅Train的优化器塌缩诊断

2026-09-11注册于B完成之后。B六组无IoU增益，T3/T4阶段门失败；T4/image几乎无有效输出，权重范数衰减至约1e−20。第一步零输出初始化使前层任务梯度为0而Adam coupled L2非零；这支持优化配置压制弱分支的解释，但尚需受控干预。D不是第二次评估B内部holdout，也不改变B结果。

只比较三个优化器处理，各含原六头：原Adam(weight_decay=1e−4)、Adam(weight_decay=0)、AdamW(weight_decay=1e−4)。完全相同初始状态哈希、B原样本序列前512×16个呈现、原1024步单次余弦的前512步（不是重缩放成512步余弦）、原Dice/Focal、梯度裁剪5、batch16、seed1219、相同feature/text/GT缓存。固定原32张eligible fit诊断在0/64/256/512步记录。所有读取图像索引必须为原1930张eligible fit；不重评1131张内部holdout，不读取官方Val/Test。

复现前提：原Adam组0/256/512步32张fit的每图全部原指标必须与已保存B遥测逐项完全一致；否则记录为复现失败，不作优化器结论。原heads.py字节及共享缓存、成员/序列/诊断文件SHA全部核验，R2源码不变。模型头初始参数、数据、阈值或文字规则不允许因看到B结果而调整。

判断对象是优化器对分支存活的因果影响：记录Q/K/V/offset/out参数范数、任务梯度、残差幅度、变化图像及Train诊断IoU。若无衰减或AdamW恢复T4/image非零学习且原Adam准确复现塌缩，则在此配置/预算下支持coupled L2为塌缩机制；不代表恢复后的分支已改善独立IoU。如果仍塌缩，回到表征差分尺度、投影梯度和目标相容性研究。无论哪种结果，都不自动通过完整80轮/Test门，不反复调整原B holdout。

复用原B缓存只读，不复制1.78GB缓存；新运行仅写小头/日志/诊断文件，系统盘至少预留128MB。B原权重/结果不改，shared fs不写。新完整SHA及独立tag，后台派发后断开SSH。

从B实测0.152984秒/六头一步估算：18头×512步约235秒，加60秒读缓存/诊断余量约295秒；首次预约派发后8分钟，预计完成后约3分钟。首次若已完成即关闭，只用1/2次；否则依据新实测速率预测唯一末检。最多2次、不持续SSH或轮询；末检记录结束到观察≤30分钟是否满足。成功后独立复算/归档/HF备份，保留失败证据。
