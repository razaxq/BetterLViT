# T2 固定分支干预：第二创新点的机制筛选入口

2026-09-13。用户授权按既定路线推进，最终目标为寻找并验证与 FAM-EPPA 配合的第二项结构创新。诊断本身不认领创新。

固定 T2 seed1219 Best69（源 488ef093de80df71ee77741a8c7ee7b938c7d6b5），在全部1429张Val、batch16、严格概率>0.5上比较三种前向：真实文字、新增分支关闭、仅新增分支使用同有效token长度的不同报告。原有ViT及EPPA文字通路始终使用真实报告。无训练、无阈值搜索、无Test读取。

错配仅按文件名、有效token长度和token内容确定，禁止使用图像、mask或模型预测选择供体。按文件名排序循环选取同长度且编码内容不同的首个报告；若无此供体则保持原报告并明确报告不可干预数量，错配分析同时报告可干预子集。相同报告不算错配。不得由错配构造新目标mask。

严格核验模型来源、架构、无LoRA与无boundary loss；真实文字的逐图整数TP、预测面积、GT面积必须复现历史导出。检查模型参数及buffer前后哈希一致、首批重复输出完全相同，保存供体映射和代码哈希。各组计算macro IoU/Dice/Precision/Recall/Brier、TP/FP/FN及固定最小面积组(<=2075像素)。报告描述性分组配对bootstrap，不把图像区间等同于跨训练种子证据。

先判断关闭新增分支是否缓解小病灶损害。关闭分支是同一训练后T2的干预，不等价于重新训练R2；错配退步不单独证明语义理解。若关闭仍不能缓解小病灶损害，先审视优化干扰，不直接启动短语拒配训练。若有支持，再冻结普通完整短语注意力与同容量空匹配版本的源码和80轮配方。首轮不添加复杂OT或额外损失；EPPA与R2配方保持，最终结构消融必须使用统一的Train/Val选择与Test评估协议。

最终目标的验收包含实际macro IoU收益、与EPPA组合的独立消融、多个配对种子、机制对照及与既有工作的实质区别。不能保证研究成功；失败时记录证据并调整问题定位，不把已失败方案改名充当创新。
