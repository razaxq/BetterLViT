# F：用户要求的固定模型 Test 补测

用户于 2026-09-12 明确要求“试试测试集”。本次授权覆盖 F 的 Test 补测，原 Train 内筛选失败结论保留。沿用训练源 a76e1005d5f4b028f5e991c2cf79564b79e66203 的全部四组、五折最终 2048 步模型头，不再训练或选折。

完整 2113 张 Test，R2 seed1219 / Best67，原正确文本，224×224、batch16、固定严格 >0.5。R2 的逐图 IoU/Dice/Precision/Recall 与历史同一检查点 Test 导出必须精确一致。四组各自报告五个模型在同一 Test 上的逐图 macro 指标，再对五个模型等权平均；该均值不是预测集成，也不是五个独立随机种子。主要关注 Train 内预先选定的 fine_mass，同时完整报告另外三组。

保留原 1024 个不确定点、fp16 特征量化后转 float32、67→128→64→1 结构和原投影实现。冻结所有权重，模型和头前后 state hash 一致；候选选择不接触标签，标签仅用于指标。候选外概率严格不变，保量误差每图 <=0.02。新目录单独允许 Test，原 F 的 Train-only guard 和历史状态不修改。

每个模型头完整 Test 只推理一次。不得使用 Test 选择阈值、轮次、残差强度、折或新配置。对每图先平均五头的指标增量，再按可识别 sub-S 患者 ID 分组 bootstrap 10000 次，其他文件名各自成组；描述性 95% 区间未校正多重比较，不能证明全体患者独立或跨训练种子稳定。

提交并固定评估源 SHA/tag 后后台运行，SSH 随即断开。根据既有 2113 图 R2 推理约 160 秒及四头实测速度，预留五折新增头计算/汇总，首次收集设为提交后 480 秒；只收集完成状态和结果，不持续连接。若意外未结束，仅根据写出的 ETA 安排第二次。无新训练，历史训练检查次数不变。

保存逐图计数/指标/错误转移、20头状态和文件哈希、R2历史一致性、完整各折汇总及源代码；独立复算后推送 GitHub，并按评估源短 SHA 添加至 HF Bucket。共享 fs 不写、不清理旧模型或缓存。
