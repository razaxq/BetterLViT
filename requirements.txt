# LViT baseline (QaTa-COV19) — RTX 5090 / CUDA 12 compatible.
#
# The legacy mxnet `bert-embedding` package has been removed; text features are
# precomputed with HuggingFace bert-base-uncased (see tools/precompute_text_emb.py).
#
# Install PyTorch from the CUDA 12.8 wheel index first, e.g.:
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch torchvision
# then:  pip install -r requirements.txt

torch>=2.6
torchvision>=0.21
transformers>=4.40        # bert-base-uncased via AutoModel/BertModel

numpy
scipy
pandas
openpyxl                  # required by pandas.read_excel for the *.xlsx text files
opencv-python
scikit-image
scikit-learn              # utils.py: jaccard_score, roc_auc_score
matplotlib                # test_model.py visualisation
ml-collections            # Config.get_CTranS_config()
Pillow
timm                      # nets: timm.models.layers.DropPath (pin if the import path moves)
tensorboardX              # train_model.py: SummaryWriter
torchinfo
tqdm
