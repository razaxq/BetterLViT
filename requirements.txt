# BetterLViT — Python 3.11, CUDA 12.8, RTX 5090 (Blackwell, sm_120)
# Install with: pip install -r requirements.txt
# For PyTorch with CUDA 12.8, use the official index:
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch torchvision

# --- Core DL stack ---
torch>=2.6
torchvision>=0.21
torchaudio>=2.6

# --- HF / text encoders ---
transformers>=4.40
tokenizers>=0.19
safetensors>=0.4
peft>=0.11           # LoRA for partial fine-tuning of BiomedCLIP

# --- Training infra ---
accelerate>=0.30
timm>=1.0
wandb>=0.17
torchmetrics>=1.4
tensorboard>=2.16    # backs torch.utils.tensorboard.SummaryWriter (replaces tensorboardX)

# --- Medical imaging ---
monai>=1.3
medpy>=0.5           # HD95 and other medical metrics
SimpleITK>=2.3

# --- Data / augmentation ---
albumentations>=1.4
opencv-python>=4.9
Pillow>=10.0
scikit-image>=0.22
scikit-learn>=1.3
scipy>=1.11
numpy>=1.24
pandas>=2.0
openpyxl>=3.1        # read *_text.xlsx (replaces xlrd which dropped xlsx support)
h5py>=3.10
tifffile>=2024.1

# --- Utilities ---
ml-collections>=0.1.1
einops>=0.7
tqdm>=4.66
matplotlib>=3.8
seaborn>=0.13
pyyaml>=6.0
thop>=0.1
torchinfo>=1.8
