# Three text-guided comparators on the fixed eight QaTa-COV19-v2 images

Authorized 2026-09-20: user requests three image+text inference comparators and permits server training if needed. Existing official MMI-UNet inference counts as one. Add GuideDecoder (LanGuideMedSeg, MICCAI 2023) and DD-CMD (official MICCAI 2026 repository). Both forward passes consume per-image clinical text. Do not include image-only LEAF, text-free SGSeg, or substitute generic U-Net/TransUNet. STPNet inspected but not selected: released inference selects from a shared text bank. TGCAM repository currently has only Readme. No training for these rejected candidates.

Official sources: https://github.com/Junelin2333/LanGuideMedSeg-MICCAI2023 at acfd134cf51067a145904d7031b631a6119dbc3f; https://github.com/maklachur/DD-CMD at 27387c47163971448f8fcdf5e37f310509dbd948. No verified full task checkpoint available for either. Architecture files are copied without alteration and hashed. Experiment runner has separate commit and tag.

Run serially on existing RTX 4090 D, one seed 1219 each. Train5716 / Val1429 / Test2113 must match existing project folders, paired by exact filename. Preserve all original captions. No extra 80/20 subdivision in GuideDecoder. 224px, text length24; use each author's image transforms and frozen text encoder. Guide: pretrained ConvNeXt-tiny and CXR-BERT-specialized, batch16 (official32 adjusted for GPU memory), AdamW lr3e-4 wd0.01, original DiceCELoss on probability output, max100 epochs. DD-CMD: pretrained ConvNeXt-tiny and BiomedBERT abstract-fulltext, batch8, AdamW lr5e-5 wd0.01, original DiceCELoss(sigmoid=True) on logits, max160 epochs. Both retain official CosineAnnealingLR T_max200 eta_min1e-6, with no warm restarts; do not impose the BetterLViT recipe on foreign methods. Fixed budgets, no early stop, choose Best by Val per-image macro IoU >0.5 (documented protocol adaptation from original trainers). FP32, no LoRA. Val does not train. Test is accessed for predictions only after training and Best selection. Full fixed-Best Test once, save all per-image metrics and raw masks/probabilities for eight selected cases.

Isolated system-site-packages venv: reuse CUDA torch, override transformers4.44.2 and MONAI1.3.2 for server Python3.12 compatibility. Guide dataset's unused removed AddChanneld import is the only upstream dataset compatibility edit; model architecture untouched. These are project-protocol retrainings, not exact published-score reproductions. MMI remains author-checkpoint inference with unverified exact training filenames. No score-based method/seed switching.

Before training: verify split counts/disjoint filename sets, captions, image and mask dimensions; real Train-only GPU forward/backward, finite nonzero gradients, text sensitivity, and unchanged model source hashes. Preflight uses a disposable model and is reseeded before formal initialization. Keep Best/Last, full source/runtime/config/hash/metrics. Queue automatically advances only after train/Val/Test success; failure stops without automatic formal rerun. Per-model active inspection max2, counts persisted before connection; preflight is not formal training. Preserve all existing weights.
