# P8BA: R2 plus binding-repaired auxiliary supervision only

User requested this configuration first on 2026-09-15. This is one new matched control, not a launch of the full ablation matrix.

Base: P8B2 source `c6e52a9b13576cab9e2451c550d03ad274bc09e2`. The only intended training mechanism change is disabling the four RACE residual routes. All RACE modules are instantiated in the original order with the same initialization; each disabled route returns the original skip tensor irrespective of its strength. Auxiliary text, visual-zone and positive-consistency losses are unchanged, including their combined weight 0.05. Visual auxiliary gradients still reach the CNN encoder. Residual and strength parameters do not receive gradients. The frozen text encoder remains frozen. At inference the decoder receives unmodified CNN skips; auxiliary outputs do not alter predictions.

Protocol: existing QaTa-COV19-v2 split; 5716 Train, 1429 Val, 2113 Test. FAM-EPPA V4-B; no LoRA, boundary loss or external visual prior; Dice/Focal; Adam, weight decay0.0001; legacy synchronized augmentation; batch16, seed1219, 80 epochs, from scratch. Single cosine3e-4 to1e-6. Val macro-IoU selects Best; threshold strictly greater than0.5. Binding repair, legacy fallback on uncovered reports and original count targets remain exactly P8B2's. No auxiliary subloss or weight search.

Comparisons fixed before training: P8BA versus R2 estimates the auxiliary package's effect. P8B2 versus P8BA estimates routing's effect under the same repaired auxiliary supervision. Report both Val and Test, all macro IoU/Dice/Precision/Recall/Brier metrics, paired image intervals and seed limitations. Preserve all existing P8R2/P8B2 results and their failed Val discovery gates. Test has already been accessed for prior variants; these comparisons are exploratory ablation evidence, not a new untouched-Test confirmation or an established second innovation.

Following the standing user request to evaluate Test after every completed training, this run automatically exports the full Best Val first, then evaluates that same checkpoint on Test at0.5. Train has Test access disabled; only the dedicated final Test process enables it. Never choose a checkpoint or threshold using Test. Separate training/Val/Test logs, return codes, full source SHA and experiment tag are mandatory.

Preflight: CPU regression against the frozen original module; identical initial weights and outputs; route bypass remains identity even with nonzero strength; auxiliary gradients reach CNN and evidence/text heads while residual/strength gradients are absent; both actual evaluation CLI modes accept the new profile. Real Train CUDA batch16 checks run twice, with identical outputs/losses and finite gradients; initial output/loss must match the recorded P8B2 preflight. GPU and scratch storage checks precede launch. No training checkpoint is used for initialization.
